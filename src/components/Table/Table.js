import React, { Component, PropTypes } from 'react';

class Table extends Component {
  render() {
    const { className, head, body } = this.props;

    return <table className={classNames(className, )}>

    </table>;
  }
}

export default Table;
