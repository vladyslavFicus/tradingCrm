{
  "name": "@crm/common",
  "version": "0.1.0",
  "description": "",
  "private": true,
  "license": "MIT",
  "main": "./src/libraryIndex.ts",
  "scripts": {
    "start": "npm run schema && concurrently -k \"npm:codegen --\"",
    "codegen": "graphql-codegen",
    "schema": "graphql-codegen -c ../../codegen.download.yml",
    "start:playground": "react-scripts start"
  },
  "dependencies": {},
   "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
