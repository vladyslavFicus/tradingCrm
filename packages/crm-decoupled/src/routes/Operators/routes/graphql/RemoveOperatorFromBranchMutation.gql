mutation RemoveOperatorFromBranchMutation(
  $branchId: String!
  $operatorId: String!
) {
  operator {
    removeOperatorFromBranch (
      branchId: $branchId
      operatorId: $operatorId
    )
  }
}
