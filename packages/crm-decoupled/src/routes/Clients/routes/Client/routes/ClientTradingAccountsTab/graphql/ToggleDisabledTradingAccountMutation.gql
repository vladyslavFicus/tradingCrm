mutation ToggleDisabledTradingAccountMutation(
  $accountUUID: String!
  $readOnly: Boolean!
) {
  tradingAccount {
    toggleDisabled(
      accountUUID: $accountUUID,
      readOnly: $readOnly,
    )
  }
}
