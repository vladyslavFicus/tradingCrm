mutation ActivatePendingOrderMutation(
  $orderId: Int!
  $activationPrice: Float!
) {
  tradingEngine {
    activatePendingOrder(
      orderId: $orderId
      activationPrice: $activationPrice
    )
  }
}
