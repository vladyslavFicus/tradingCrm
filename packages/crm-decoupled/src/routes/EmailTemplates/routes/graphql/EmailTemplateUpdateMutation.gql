mutation EmailTemplateUpdateMutation(
  $name: String!
  $subject: String!
  $text: String!
  $id: ID!
) {
  emailTemplates {
    updateEmailTemplate(
      id: $id
      name: $name
      text: $text
      subject: $subject
    )
  }
}
