query PaymentSystemsProviderQuery($args: PaymentSystemsProvider_Input) {
  settings {
    paymentSystemsProvider(args: $args) {
      totalPages
      totalElements
      last
      number
      content {
        paymentSystem
        isFavourite
      }
    }
  }
}
