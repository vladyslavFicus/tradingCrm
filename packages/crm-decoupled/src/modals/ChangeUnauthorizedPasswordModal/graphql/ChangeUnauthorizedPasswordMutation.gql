mutation ChangeUnauthorizedPasswordMutation(
  $uuid: String!
  $currentPassword: String!
  $newPassword: String!
) {
  auth {
    changeUnauthorizedPassword (
      uuid: $uuid
      currentPassword: $currentPassword
      newPassword: $newPassword
    )
  }
}
